// ====================================================================
// Questo file JavaScript è stato semplificato.
// La gestione della lingua, del titolo e l'evidenziazione della nav
// vengono ora gestiti interamente dal template Jinja2 (server-side).
//
// Questo file rimane come placeholder per future logiche client-side
// (es. animazioni, interazioni che non riguardano routing/lingua).
// ====================================================================

console.log("Jinja2 templating active. JS language and routing logic is now server-side.");

/**
 * La funzione handlePageLoad e PAGE_CONFIG non sono più necessarie.
 * L'attivazione dei link e la gestione dei titoli avviene in base.html
 * e nei template specifici.
 */
document.addEventListener('DOMContentLoaded', () => {
    // Aggiungi qui eventuali inizializzazioni JS globali (es. analitiche, animazioni)
});